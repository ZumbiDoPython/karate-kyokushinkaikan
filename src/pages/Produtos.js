import React from 'react';
import InstitutionalPage from '../components/InstitutionalPage';

const Produtos = () => <InstitutionalPage slug="produtos" withSidebar={false} />;

export default Produtos;
