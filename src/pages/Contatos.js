import React from 'react';
import InstitutionalPage from '../components/InstitutionalPage';

const Contatos = () => <InstitutionalPage slug="contatos" withSidebar={false} />;

export default Contatos;
